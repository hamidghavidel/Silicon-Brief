version: "3.8"

services:
  silicon-brief:
    build: .
    container_name: silicon-brief
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./silicon-brief.db:/app/silicon-brief.db
    restart: unless-stopped
