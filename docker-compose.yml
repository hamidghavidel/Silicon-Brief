version: "3.8"

services:
  silicon-brief:
    build: .
    container_name: silicon-brief
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_CHANNEL_ID=${TELEGRAM_CHANNEL_ID}
    volumes:
      - ./data:/root/data
    restart: unless-stopped
